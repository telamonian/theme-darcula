sudo: false
dist: trusty

language: python
python: 3.6

cache:
  pip: true
  directories:
    - /home/travis/.yarn-cache/

install:
  - bash source ./.ci_funcs.sh && ci_install
script:
  - bash source ./.ci_funcs.sh && ci_script
